use anyhow::Result;
use std::path::Path;

pub fn prepare_pk(
    _privkey_path: &Path,
    _ckb_rpc_url: &str,
    _ckb_count: u32,
    _nodes_count: u32,
    _output_dir: &Path,
    _poa_config_path: &Path,
    _rollup_config_path: &Path,
) -> Result<()> {
    unimplemented!()
}
